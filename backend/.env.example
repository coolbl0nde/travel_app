# Since the ".env" file is gitignored, you can use the ".env.example" file to
# build a new ".env" file when you clone the repo. Keep this file up-to-date
# when you add new variables to `.env`.

# This file will be committed to version control, so make sure not to have any
# secrets in it. If you are cloning this repo, create a copy of this file named
# ".env" and populate it with your secrets.

# When adding additional environment variables, the schema in "src/config/index.ts"
# should be updated accordingly.

# NGROK
NGROK_URL=

# Application URLs
API_URL=http://localhost:3001
WEB_URL=http://localhost:3002

# JWT Secret
# You can generate a new Token secret on the command line with the following comand:
# openssl rand -base64 32
JWT_SECRET=

# ADMIN KEY
# You can generate a new admin key on the command line with the following comand:
# openssl rand -base64 32
ADMIN_KEY=

# MongoDB
MONGO_URI=mongodb://root:root@localhost:27017/api-development?authSource=admin&replicaSet=rs&tls=false&directConnection=true
MONGO_DB_NAME=api-development

#APIs

# OpenAI API Key
OPENAI_API_KEY=

# Resend
RESEND_API_KEY=

# Cloud storage (Digital Ocean Spaces)
CLOUD_STORAGE_ENDPOINT=https://ralli-staging.nyc3.digitaloceanspaces.com
CLOUD_STORAGE_BUCKET=ralli-staging
CLOUD_STORAGE_ACCESS_KEY_ID=
CLOUD_STORAGE_SECRET_ACCESS_KEY=

# Google
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Hubspot
HUBSPOT_CLIENT_ID=
HUBSPOT_CLIENT_SECRET=

# # Slack
SLACK_CLIENT_SECRET=
SLACK_CLIENT_ID=
SLACK_SIGNING_SECRET=